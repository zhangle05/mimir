   <!-- =================================================================== -->
   <!-- ant properties/targets for mysql                                    -->
   <!-- note: this is not a proper xml file (there is no root element)      -->
   <!--       it is intended to be imported from a *real* xml file          -->
   <!-- =================================================================== -->

   <property name="database.script.file"           value="${src.dir}/sql/${name}-firstsql.sql"/>
   <property name="database.driver.file"           value="${lib.dir}/fsjclasses.jar"/>
   <property name="database.driver.classpath"      value="${database.driver.file}"/>
   <property name="database.driver"                value="COM.FirstSQL.Dbcp.DbcpDriver"/>
   <property name="database.url"                   value="jdbc:dbcp://local"/>
   <property name="database.userid"                value="demo"/>
   <property name="database.password"              value=""/>
   <property name="database.schema"                value=""/>
   <property name="database.catalog"               value=""/>

   <property name="jboss.datasource.mapping"       value="???"/>
